pipeline {
    agent any
    stages {
        stage('Compile') {
            steps {
                sh 'echo "Build application"'
            }
        }
        stage('Package') {
            steps {
                sh 'echo "Building package"'
            }
        }
    }
}
